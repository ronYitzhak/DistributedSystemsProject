syntax = "proto3";
package protos;
import "Admin.proto";

message VoteRequest {
    string voterName = 1;
    string candidateName = 2;
    string state = 3;
}

service Voter {
    rpc Vote (VoteRequest) returns (Void) {}
    rpc MasterVote (VoteRequest) returns (Void) {}
}
